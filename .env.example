# Скопируйте этот файл в .env и при необходимости отредактируйте значения.

# Адрес OpenAI-совместимого LLM-сервера (LM Studio или облачный провайдер).
LLM_BASE_URL=http://localhost:1234/v1

# Ключ доступа. Для LM Studio — любое непустое значение.
LLM_API_KEY=lm-studio

# Идентификатор модели.
LLM_MODEL=google/gemma-4-12b-qat

# Идентификатор embedding-модели на том же сервере.
EMBEDDING_MODEL=text-embedding-qwen3-embedding-0.6b

# Размерность вектора эмбеддинга (должна совпадать с коллекцией Qdrant).
EMBEDDING_DIM=1024

# Таймаут на запрос к LLM, секунд (one-shot на локальной модели может быть долгим).
LLM_REQUEST_TIMEOUT_SECONDS=120

# Адрес Qdrant-сервера (Docker-контейнер).
QDRANT_URL=http://localhost:6333

# Название коллекции в Qdrant.
QDRANT_COLLECTION=dz4_memory

# Путь к файлу с данными (относительно корня проекта).
KNOWLEDGE_FILE=memory/knowledge.json

# Сколько результатов возвращать из поиска.
SEARCH_TOP_K=5

# Вес векторного поиска (0..1), остальное — графовый.
VECTOR_WEIGHT=0.7

# Максимальная глубина обхода графа.
GRAPH_MAX_HOP=2

# Уровень логирования: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=ERROR
